# Models

This directory is reserved for optional baseline or future learned risk models.

The first repository version does not require a trained model to run. If a placeholder or baseline model is generated locally, do not commit proprietary artifacts.